This is an onet simulation driver program. You build it with `go build` and run it
in your local dev enviroment.

You run it like this: `./sim -platform deterlab run.toml`

You can make other .toml files.